# ========================================
# NPM Configuration for UberFix.shop
# ========================================

# Registry
registry=https://registry.npmjs.org/

# Enable legacy peer dependencies for React 19 compatibility
legacy-peer-deps=true
auto-install-peers=true
strict-peer-dependencies=false
