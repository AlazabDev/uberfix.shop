// Re-export all types from the main data file
export type { ChatMessage, NavItem, Intent, ConversationFlow, ConversationStep, ProductService } from "./chatbotData.full";

// NAV_ITEMS re-export
export { NAV_ITEMS } from "./chatbotData.full";
